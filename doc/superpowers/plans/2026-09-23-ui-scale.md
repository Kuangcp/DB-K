# 全局界面缩放（UI Scale）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户用一个持久化的「界面缩放」比例把整个 Compose 界面（字体、图标、边框）等比放大，默认 100% 不改变现状。

**Architecture:** 在每棵独立窗口 composition 的根覆盖 `LocalDensity`（只乘 `density`，不动 `fontScale`），使全部 `dp`/`sp` 等比缩放。用一个全局可观察对象 `UiScaleState` 广播倍率，用一个幂等的 `ProvideUiScale`（读 `LocalUiScale` 还原系统密度后再叠加）避免 `DialogWindow` 继承父 composition 时把倍率平方。设置窗口的滑块实时预览、取消回退、保存落盘到 `<dataDir>/app.properties` 的 `ui.scale`。

**Tech Stack:** Kotlin 2.4.0 / Compose Multiplatform 1.12.0 / JDK 25（jbr）/ Gradle 9.4.1（无 wrapper）/ kotlin.test + JUnit 5。

**Spec:** `doc/superpowers/specs/2026-09-23-ui-scale-design.md`

## Global Constraints

- 工具链固定：`JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr`，Gradle `/home/zk/.sdkman/candidates/gradle/9.4.1/bin`（不引入 wrapper）。所有命令前先 export（见下）。
- 编译器 `allWarningsAsErrors`：**不得留下未使用的 import**，否则编译失败。
- 不新增任何依赖。
- i18n：所有用户可见文案必须走 `Str`，**zh + en 同步**；占位符 `{0}` 从零连续。
- 持久化分层：应用级标量偏好写 `<dataDir>/app.properties`，本特性 key 固定为 `ui.scale`（乘算在系统密度之上）。
- **禁止 UI 自动化验证**（AGENTS）：不得用 xdotool/截图/沙箱数据目录。UI 行为只写「人工验证步骤」交给用户执行。
- 每个 Task 结束提交一次（frequent commits）。
- 自检临时目录必须自清理（本计划不新建临时目录）。

前置 export（每个执行 step 的 shell 会话都需要）：

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
```

## Review Focus

按 spec 推断、但自动化测试覆盖不到、最可能坑用户的 5 类情况（每行在对应 Task 里有落点）：

1. **`scale ≠ 100%` 时的命中对齐**：点击树节点、拖分隔条、编辑器点选/拖选应与视觉一致，不偏移。
   归 Task 3（实现）+ Task 3/5 人工验证步骤。
2. **弹层（下拉菜单/右键菜单/Tooltip/补全）的缩放与定位**：文字与命中区同步放大，不跑位。
   归 Task 5 人工验证步骤（子 composition 继承密度）。
3. **对话框内容溢出/裁剪**：放大后固定 Dp 尺寸的 `DialogWindow` 不能出现内容被裁。
   归 Task 4（设置窗自身）与 Task 5（其余弹窗）的尺寸同比放大与人工验证。
4. **取消未回退**：实时预览后点「取消/关闭」必须回到打开设置前的比例，且不落盘。
   归 Task 4 的 `onDismiss` 回退接线与人工验证。
5. **`app.properties` 读-改-写互相覆盖**：写 `ui.scale` 不能丢 `language`，反之亦然。
   归 Task 1 的 `UiScalePrefsTest`（及 Task 4 的落盘人工复核）。

---

### Task 1: 缩放偏好持久化（`UiScalePrefs`）

**Files:**
- Create: `src/main/kotlin/app/settings/UiScalePrefs.kt`
- Test: `src/test/kotlin/app/settings/UiScalePrefsTest.kt`

**Interfaces:**
- Consumes: `db.AppPaths.dataDirectory()`（已有）。
- Produces:
  - `object UiScalePrefs`：常量 `MIN_SCALE=1.0f`、`MAX_SCALE=2.0f`、`STEP=0.05f`、`DEFAULT_SCALE=1.0f`；
    `fun sanitized(value: Float): Float`；`fun load(): Float`；`fun save(scale: Float)`；
    `internal fun load(f: File): Float`；`internal fun save(f: File, scale: Float)`。

- [ ] **Step 1: 写失败测试 `UiScalePrefsTest`**

```kotlin
package app.settings

import org.junit.jupiter.api.io.TempDir
import java.nio.file.Path
import java.util.Properties
import kotlin.test.Test
import kotlin.test.assertEquals

/** 全局界面缩放偏好的读写往返（纯文件，不触碰真实数据目录）。 */
class UiScalePrefsTest {

    @TempDir
    lateinit var dir: Path

    private val file get() = dir.resolve("app.properties").toFile()

    private fun readProps(): Properties =
        Properties().apply { file.inputStream().use { load(it) } }

    @Test
    fun `missing file yields default 100 percent`() {
        assertEquals(1.0f, UiScalePrefs.load(file))
    }

    @Test
    fun `saves and loads round trip`() {
        UiScalePrefs.save(file, 1.5f)
        assertEquals(1.5f, UiScalePrefs.load(file))
    }

    @Test
    fun `out of range is clamped on save`() {
        UiScalePrefs.save(file, 5.0f)
        assertEquals(UiScalePrefs.MAX_SCALE, UiScalePrefs.load(file))
        UiScalePrefs.save(file, 0.1f)
        assertEquals(UiScalePrefs.MIN_SCALE, UiScalePrefs.load(file))
    }

    @Test
    fun `non finite falls back to default`() {
        assertEquals(UiScalePrefs.DEFAULT_SCALE, UiScalePrefs.sanitized(Float.NaN))
        assertEquals(UiScalePrefs.DEFAULT_SCALE, UiScalePrefs.sanitized(Float.POSITIVE_INFINITY))
    }

    @Test
    fun `invalid stored value falls back to default`() {
        file.writeText("ui.scale=abc\n")
        assertEquals(UiScalePrefs.DEFAULT_SCALE, UiScalePrefs.load(file))
    }

    @Test
    fun `saving scale preserves existing language key`() {
        file.parentFile?.mkdirs()
        file.writeText("language=en\n")
        UiScalePrefs.save(file, 1.25f)
        val props = readProps()
        assertEquals("en", props.getProperty("language"))
        assertEquals("1.25", props.getProperty("ui.scale"))
    }

    @Test
    fun `language written after scale does not clobber ui scale`() {
        // 模拟 LanguagePrefs.save 的读-改-写：先 load 再补 language key 后 store
        UiScalePrefs.save(file, 1.5f)
        val props = readProps().apply { setProperty("language", "zh") }
        file.outputStream().use { props.store(it, "db-k app preferences") }
        assertEquals(1.5f, UiScalePrefs.load(file))
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "app.settings.UiScalePrefsTest"
```

Expected: 编译失败（`UiScalePrefs` 未定义）。

- [ ] **Step 3: 实现 `UiScalePrefs`**

```kotlin
package app.settings

import db.AppPaths
import java.io.File
import java.util.Properties

/**
 * 全局界面缩放偏好（`<dataDir>/app.properties`，key `ui.scale`）。
 *
 * 与 [LanguagePrefs] 同放一个文件：每次读-改-写，互不覆盖其它 key。
 * `1.0` = 100%（不缩放）；缩放与系统已有密度是**乘算**关系。
 */
object UiScalePrefs {

    private const val KEY_UI_SCALE = "ui.scale"

    const val MIN_SCALE = 1.0f
    const val MAX_SCALE = 2.0f
    const val STEP = 0.05f
    const val DEFAULT_SCALE = 1.0f

    /** 夹到 [MIN_SCALE, MAX_SCALE]；NaN/Infinity 回落默认。 */
    fun sanitized(value: Float): Float =
        if (value.isFinite()) value.coerceIn(MIN_SCALE, MAX_SCALE) else DEFAULT_SCALE

    private fun prefsFile(): File = AppPaths.dataDirectory().resolve("app.properties").toFile()

    fun load(): Float = load(prefsFile())

    fun save(scale: Float) = save(prefsFile(), scale)

    // ---------- 纯文件实现（便于单测注入临时文件，不触碰真实数据目录） ----------

    internal fun load(f: File): Float {
        if (!f.exists()) return DEFAULT_SCALE
        return runCatching {
            val props = Properties()
            f.inputStream().use { props.load(it) }
            val raw = props.getProperty(KEY_UI_SCALE)?.toFloatOrNull()
            if (raw == null) DEFAULT_SCALE else sanitized(raw)
        }.getOrDefault(DEFAULT_SCALE)
    }

    internal fun save(f: File, scale: Float) {
        runCatching {
            val props = Properties()
            if (f.exists()) f.inputStream().use { props.load(it) }
            props.setProperty(KEY_UI_SCALE, sanitized(scale).toString())
            f.parentFile?.mkdirs()
            f.outputStream().use { props.store(it, "db-k app preferences") }
        }
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "app.settings.UiScalePrefsTest"
```

Expected: 7 个 test PASS。

- [ ] **Step 5: 提交**

```bash
git add src/main/kotlin/app/settings/UiScalePrefs.kt src/test/kotlin/app/settings/UiScalePrefsTest.kt
git commit -m "feat(ui-scale): UiScalePrefs 持久化（app.properties ui.scale）"
```

---

### Task 2: i18n 文案（zh + en）

**Files:**
- Modify: `src/main/kotlin/i18n/Str.kt`（枚举，约 `SettingsPreview` 之后）
- Modify: `src/main/kotlin/i18n/CatalogZh.kt`（约 `Str.SettingsPreview` 分支之后）
- Modify: `src/main/kotlin/i18n/CatalogEn.kt`（约 `Str.SettingsPreview` 分支之后）
- Test: `src/test/kotlin/i18n/I18nCatalogTest.kt`（已有，无需改）

**Interfaces:**
- Produces: `Str.SettingsUiScale`（含占位符 `{0}`）、`Str.SettingsUiScaleHint`。

- [ ] **Step 1: 运行现有 i18n 测试（基线，应通过）**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "i18n.I18nCatalogTest"
```

Expected: PASS（基线）。

- [ ] **Step 2: 在 `Str.kt` 枚举加入两个 key**

`Str.kt` 中找到：

```kotlin
    SettingsEditorFontSize,
    SettingsPreview,
```

改为：

```kotlin
    SettingsEditorFontSize,
    SettingsPreview,
    SettingsUiScale,
    SettingsUiScaleHint,
```

- [ ] **Step 3: 在两个 catalog 加分支**

`CatalogZh.kt` 中找到：

```kotlin
    Str.SettingsPreview -> "预览"
```

改为：

```kotlin
    Str.SettingsPreview -> "预览"
    Str.SettingsUiScale -> "界面缩放（{0}%）"
    Str.SettingsUiScaleHint -> "放大整个界面（字体、图标、边框），即时生效，取消不改"
```

`CatalogEn.kt` 中找到：

```kotlin
    Str.SettingsPreview -> "Preview"
```

改为：

```kotlin
    Str.SettingsPreview -> "Preview"
    Str.SettingsUiScale -> "UI scale ({0}%)"
    Str.SettingsUiScaleHint -> "Scales the entire UI (text, icons, borders). Applies immediately; cancelling reverts."
```

- [ ] **Step 4: 运行 i18n 测试，确认通过**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "i18n.I18nCatalogTest"
```

Expected: PASS（非空、占位符一致且从 0 连续）。

- [ ] **Step 5: 提交**

```bash
git add src/main/kotlin/i18n/Str.kt src/main/kotlin/i18n/CatalogZh.kt src/main/kotlin/i18n/CatalogEn.kt
git commit -m "feat(ui-scale): 界面缩放 i18n 文案（zh/en）"
```

---

### Task 3: 缩放状态、提供者与主窗口接线

**Files:**
- Create: `src/main/kotlin/app/state/UiScaleState.kt`
- Create: `src/main/kotlin/app/ui/UiScale.kt`
- Test: `src/test/kotlin/app/ui/UiScaleTest.kt`
- Modify: `src/main/kotlin/app/core/Main.kt`（`main()` 初始化、`AppRoot` 默认尺寸与 decoration、包裹 `AppBody`、import）

**Interfaces:**
- Consumes: `app.settings.UiScalePrefs`（Task 1）。
- Produces:
  - `object UiScaleState { var scale: Float (只读); fun set(value: Float) }`
  - `val LocalUiScale: ProvidableCompositionLocal<Float>`
  - `internal fun scaledDensity(currentDensity: Float, applied: Float, target: Float): Float`
  - `@Composable fun ProvideUiScale(content: @Composable () -> Unit)`
  - `@Composable fun scaledSize(width: Dp, height: Dp): DpSize`
  - `@Composable fun scaledSize(size: DpSize): DpSize`
  - `@Composable fun ScaledDialogWindow(onCloseRequest: () -> Unit, state: DialogState, title: String, resizable: Boolean = true, onPreviewKeyEvent: (KeyEvent) -> Boolean = { false }, content: @Composable DialogWindowScope.() -> Unit)`（Task 4/5 使用）

- [ ] **Step 1: 写失败测试 `UiScaleTest`（幂等密度换算）**

```kotlin
package app.ui

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * [scaledDensity] 的幂等性：祖先窗口已放大后再包一次不得把倍率平方。
 * 这是 DialogWindow 可能继承父 composition 密度时的关键保护。
 */
class UiScaleTest {

    @Test
    fun `no previous scale applies target directly`() {
        assertEquals(1.5f, scaledDensity(currentDensity = 1f, applied = 1f, target = 1.5f))
    }

    @Test
    fun `already applied scale does not compound`() {
        assertEquals(1.5f, scaledDensity(currentDensity = 1.5f, applied = 1.5f, target = 1.5f))
    }

    @Test
    fun `system hidpi density is preserved once and idempotently`() {
        val once = scaledDensity(1.25f, 1f, 1.5f)
        assertEquals(1.875f, once)
        assertEquals(1.875f, scaledDensity(once, 1.5f, 1.5f))
    }

    @Test
    fun `target one leaves system density unchanged`() {
        assertEquals(1.25f, scaledDensity(1.25f, 1f, 1f))
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "app.ui.UiScaleTest"
```

Expected: 编译失败（`scaledDensity` 未定义）。

- [ ] **Step 3: 实现 `UiScaleState`**

```kotlin
package app.state

import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.setValue
import app.settings.UiScalePrefs

/**
 * 全局界面缩放（1.0 = 100%）的单一可观察来源。
 *
 * 主窗口与各 DialogWindow 是不同 composition，各自读取本对象 → 缩放变化时各自重组。
 * 读写经 [set]（内部按 [UiScalePrefs] 夹取）。
 */
object UiScaleState {
    var scale by mutableStateOf(UiScalePrefs.DEFAULT_SCALE)
        private set

    fun set(value: Float) {
        scale = UiScalePrefs.sanitized(value)
    }
}
```

- [ ] **Step 4: 实现 `app/ui/UiScale.kt`**

```kotlin
package app.ui

import androidx.compose.runtime.Composable
import androidx.compose.runtime.CompositionLocalProvider
import androidx.compose.runtime.staticCompositionLocalOf
import androidx.compose.ui.input.key.KeyEvent
import androidx.compose.ui.platform.LocalDensity
import androidx.compose.ui.unit.Density
import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.window.DialogState
import androidx.compose.ui.window.DialogWindow
import androidx.compose.ui.window.DialogWindowScope
import app.state.UiScaleState

/** 当前子树已经叠加过的缩放倍率（默认 1 = 未缩放）。用于让 [ProvideUiScale] 幂等。 */
val LocalUiScale = staticCompositionLocalOf { 1f }

/**
 * 幂等密度换算：当前密度已叠加 [applied] 倍缩放，先还原系统密度再叠加 [target]。
 * [applied] 为 0 属异常输入，按未缩放处理，避免除零得到 Infinity。
 */
internal fun scaledDensity(currentDensity: Float, applied: Float, target: Float): Float =
    if (applied == 0f) currentDensity * target else currentDensity / applied * target

/**
 * 在当前 composition 根提供缩放后的 LocalDensity。每个独立窗口都要各包一层；
 * **幂等**：DialogWindow 可能已从父 composition 继承放大后的密度，重复包裹不会平方。
 */
@Composable
fun ProvideUiScale(content: @Composable () -> Unit) {
    val current = LocalDensity.current
    val applied = LocalUiScale.current
    val scale = UiScaleState.scale
    CompositionLocalProvider(
        LocalDensity provides Density(scaledDensity(current.density, applied, scale), current.fontScale),
        LocalUiScale provides scale,
        content = content,
    )
}

/** 固定尺寸窗口/对话框的 Dp 尺寸同比放大，避免缩放后内容溢出固定窗口。 */
@Composable
fun scaledSize(width: Dp, height: Dp): DpSize =
    DpSize(width * UiScaleState.scale, height * UiScaleState.scale)

@Composable
fun scaledSize(size: DpSize): DpSize = scaledSize(size.width, size.height)

/**
 * 与 [DialogWindow]（`onCloseRequest/state/title/resizable/onPreviewKeyEvent` 重载）同参，
 * 但在内容层套一层 [ProvideUiScale]。调用方把 `rememberDialogState` 的尺寸用 [scaledSize] 放大。
 */
@Composable
fun ScaledDialogWindow(
    onCloseRequest: () -> Unit,
    state: DialogState,
    title: String,
    resizable: Boolean = true,
    onPreviewKeyEvent: (KeyEvent) -> Boolean = { false },
    content: @Composable DialogWindowScope.() -> Unit,
) {
    DialogWindow(
        onCloseRequest = onCloseRequest,
        state = state,
        title = title,
        resizable = resizable,
        onPreviewKeyEvent = onPreviewKeyEvent,
    ) {
        ProvideUiScale { content() }
    }
}
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle test --tests "app.ui.UiScaleTest"
```

Expected: 4 个 test PASS。

- [ ] **Step 6: 主窗口接线（`Main.kt`）**

(a) import 区加入（与 `app.settings.*` / `app.state.*` / `app.ui.*` 分组一致）：

```kotlin
import app.settings.UiScalePrefs
import app.state.UiScaleState
import app.ui.ProvideUiScale
```

(b) `main()` 中，紧随 `I18n.lang = LanguagePrefs.load() ?: Lang.system()` 之后：

```kotlin
    // 全局界面缩放：启动即确定（存档优先，默认 100%）；供所有窗口/对话框读取
    UiScaleState.set(UiScalePrefs.load())
```

(c) `AppRoot` 中，找到：

```kotlin
    val density = LocalDensity.current.density
    val windowState = rememberWindowState(
        width = Dp((geo?.width ?: 1180) / density),
        height = Dp((geo?.height ?: 760) / density),
```

改为：

```kotlin
    val density = LocalDensity.current.density
    // 全局界面缩放：无存档几何时按比例放大默认窗口，避免高分辨率屏首次启动过于局促
    val uiScale = UiScaleState.scale
    val defaultWidthPx = (1180 * uiScale).roundToInt()
    val defaultHeightPx = (760 * uiScale).roundToInt()
    val windowState = rememberWindowState(
        width = Dp((geo?.width ?: defaultWidthPx).toFloat() / density),
        height = Dp((geo?.height ?: defaultHeightPx).toFloat() / density),
```

(d) `AppRoot` 的 `Window(...)` 中，找到：

```kotlin
        decoration = WindowDecoration.Undecorated(4.dp),
```

改为：

```kotlin
        decoration = WindowDecoration.Undecorated(4.dp * uiScale),
```

(e) `AppRoot` 的 `Window(...)` 内容里，把 `AppBody(...)` 调用整体包进 `ProvideUiScale`：

```kotlin
        ProvideUiScale {
            AppBody(
                repository = repository,
                treeState = treeState,
                connectionsState = connectionsState,
                consoleState = consoleState,
                dialogState = dialogState,
                toastState = toastState,
                mainWindowState = windowState,
                onWindowCloseRequest = requestClose,
                treeWidthDp = treeWidthDp,
                onTreeWidthDelta = onTreeWidthDelta,
                resultFrac = resultFrac,
                onResultFracDelta = onResultFracDelta,
                treeVisible = treeVisible,
                onToggleTree = { treeVisible = !treeVisible },
            )
        }
```

> 注意：`Window` 的 `LaunchedEffect(Unit)` 图标设置块保持在外层不动，只包 `AppBody`。

- [ ] **Step 7: 编译 + 全量测试**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle compileKotlin && gradle test
```

Expected: 编译无警告错误；测试全绿。

- [ ] **Step 8: 人工验证（交给用户执行，默认 100% 行为不变）**

前置：确认 `~/.local/share/db-k/app.properties` 无 `ui.scale`（或为 1.0）。
1. `gradle run` 启动，界面与改动前完全一致（100%）。
2. 在 `app.properties` 手写 `ui.scale=1.5` 后重启：主窗口树/标签/结果区/图标/边框整体放大；
   默认窗口（无存档几何时）物理尺寸也更大。
3. 若机器上已有 `window.properties` 存档，窗口位置/物理尺寸应保持不变（缩放不改变存档几何）。

- [ ] **Step 9: 提交**

```bash
git add src/main/kotlin/app/state/UiScaleState.kt src/main/kotlin/app/ui/UiScale.kt \
        src/test/kotlin/app/ui/UiScaleTest.kt src/main/kotlin/app/core/Main.kt
git commit -m "feat(ui-scale): 全局密度缩放（UiScaleState/ProvideUiScale）+ 主窗口接线"
```

---

### Task 4: 设置界面缩放滑块 + 保存/取消接线

**Files:**
- Modify: `src/main/kotlin/app/dialog/SettingsDialog.kt`
- Modify: `src/main/kotlin/app/core/Main.kt`（设置打开快照、initial、onSave、onDismiss、preview）

**Interfaces:**
- Consumes: Task 1 `UiScalePrefs`；Task 2 `Str.SettingsUiScale/…Hint`；Task 3 `UiScaleState`、`ProvideUiScale`、`ScaledDialogWindow`、`scaledSize`。
- Produces:
  - `SettingsSnapshot` 新字段 `uiScale: Float`（默认 `UiScalePrefs.DEFAULT_SCALE`）。
  - `SettingsDialog(..., onUiScalePreview: (Float) -> Unit = {})` 新参数。

- [ ] **Step 1: `SettingsSnapshot` 加字段**

`SettingsDialog.kt` 中找到：

```kotlin
data class SettingsSnapshot(
    val editor: EditorSettings,
    val keymap: Keymap,
    /** 语言偏好；`null` = 跟随系统。 */
    val language: Lang? = null,
)
```

改为（并在 import 区加 `import app.settings.UiScalePrefs`）：

```kotlin
data class SettingsSnapshot(
    val editor: EditorSettings,
    val keymap: Keymap,
    /** 语言偏好；`null` = 跟随系统。 */
    val language: Lang? = null,
    /** 全局界面缩放；1.0 = 100%。 */
    val uiScale: Float = UiScalePrefs.DEFAULT_SCALE,
)
```

- [ ] **Step 2: `SettingsDialog` 改用 `ScaledDialogWindow` + 加 preview 参数**

import 区加：

```kotlin
import app.ui.ScaledDialogWindow
import app.ui.scaledSize
```

**删除** `import androidx.compose.ui.window.DialogWindow`（改由 `ScaledDialogWindow` 承载，留着会因 -Werror 报未使用 import）；`rememberDialogState` 保留。

找到签名：

```kotlin
fun SettingsDialog(
    visible: Boolean,
    theme: ThemeSpec,
    language: Lang,
    initial: SettingsSnapshot,
    onDismiss: () -> Unit,
    onSave: (SettingsSnapshot) -> Unit,
    onManageThemes: () -> Unit = {},
) {
```

改为：

```kotlin
fun SettingsDialog(
    visible: Boolean,
    theme: ThemeSpec,
    language: Lang,
    initial: SettingsSnapshot,
    onDismiss: () -> Unit,
    onSave: (SettingsSnapshot) -> Unit,
    onManageThemes: () -> Unit = {},
    /** 实时预览：拖动滑块时立即把比例广播到全局（取消时由调用方回退）。 */
    onUiScalePreview: (Float) -> Unit = {},
) {
```

找到：

```kotlin
    DialogWindow(
        onCloseRequest = onDismiss,
        title = t(Str.SettingsTitle),
        state = rememberDialogState(width = 760.dp, height = 520.dp),
    ) {
```

改为：

```kotlin
    ScaledDialogWindow(
        onCloseRequest = onDismiss,
        title = t(Str.SettingsTitle),
        state = rememberDialogState(size = scaledSize(760.dp, 520.dp)),
    ) {
```

并把 `SettingsBody(...)` 调用加上 `onUiScalePreview = onUiScalePreview`：

```kotlin
                    SettingsBody(
                        initial = initial,
                        onCancel = onDismiss,
                        onSave = onSave,
                        onManageThemes = onManageThemes,
                        onUiScalePreview = onUiScalePreview,
                    )
```

- [ ] **Step 3: `SettingsBody` 持有并预览缩放值**

找到 `SettingsBody` 签名：

```kotlin
private fun SettingsBody(
    initial: SettingsSnapshot,
    onCancel: () -> Unit,
    onSave: (SettingsSnapshot) -> Unit,
    onManageThemes: () -> Unit,
) {
```

改为：

```kotlin
private fun SettingsBody(
    initial: SettingsSnapshot,
    onCancel: () -> Unit,
    onSave: (SettingsSnapshot) -> Unit,
    onManageThemes: () -> Unit,
    onUiScalePreview: (Float) -> Unit = {},
) {
```

找到本地 state 声明：

```kotlin
    var keymap by remember { mutableStateOf(initial.keymap) }
    var languagePref by remember { mutableStateOf(initial.language) }
```

在其后加：

```kotlin
    var uiScale by remember { mutableFloatStateOf(initial.uiScale) }
```

找到 `GeneralSection(...)` 调用，改为：

```kotlin
                        GeneralSection(
                            fontFamily = fontFamily,
                            onFontFamilyChange = { fontFamily = it },
                            fontSize = fontSize,
                            onFontSizeChange = { fontSize = it },
                            uiScale = uiScale,
                            onUiScaleChange = { uiScale = it; onUiScalePreview(it) },
                            languagePref = languagePref,
                            onLanguageChange = { languagePref = it },
                            onManageThemes = onManageThemes,
                        )
```

找到保存按钮：

```kotlin
            TextButton(onClick = {
                onSave(SettingsSnapshot(EditorSettings.sanitized(fontFamily, fontSize), keymap, languagePref))
            }) {
```

改为：

```kotlin
            TextButton(onClick = {
                onSave(
                    SettingsSnapshot(
                        editor = EditorSettings.sanitized(fontFamily, fontSize),
                        keymap = keymap,
                        language = languagePref,
                        uiScale = uiScale,
                    ),
                )
            }) {
```

- [ ] **Step 4: `GeneralSection` 增加缩放块**

找到签名：

```kotlin
private fun GeneralSection(
    fontFamily: String,
    onFontFamilyChange: (String) -> Unit,
    fontSize: Float,
    onFontSizeChange: (Float) -> Unit,
    languagePref: Lang?,
    onLanguageChange: (Lang?) -> Unit,
    onManageThemes: () -> Unit,
) {
```

改为：

```kotlin
private fun GeneralSection(
    fontFamily: String,
    onFontFamilyChange: (String) -> Unit,
    fontSize: Float,
    onFontSizeChange: (Float) -> Unit,
    uiScale: Float,
    onUiScaleChange: (Float) -> Unit,
    languagePref: Lang?,
    onLanguageChange: (Lang?) -> Unit,
    onManageThemes: () -> Unit,
) {
```

找到「语言」标题前的位置：

```kotlin
    Text(
        t(Str.SettingsLanguage),
        style = MaterialTheme.typography.subtitle2,
        color = MaterialTheme.colors.onSurface.copy(alpha = ContentAlpha.medium),
    )
```

在其**上方**插入缩放块：

```kotlin
    Text(
        t(Str.SettingsUiScale, "%.0f".format(uiScale * 100)),
        style = MaterialTheme.typography.body2,
        color = MaterialTheme.colors.onSurface,
    )
    Slider(
        value = uiScale,
        onValueChange = onUiScaleChange,
        // 1.0..2.0、步进 0.05 → 20 段 → steps = 19
        valueRange = UiScalePrefs.MIN_SCALE..UiScalePrefs.MAX_SCALE,
        steps = 19,
        modifier = Modifier.fillMaxWidth(),
    )
    Text(
        t(Str.SettingsUiScaleHint),
        style = MaterialTheme.typography.caption,
        color = MaterialTheme.colors.onSurface.copy(alpha = ContentAlpha.medium),
    )
    Text(
        t(Str.SettingsLanguage),
        style = MaterialTheme.typography.subtitle2,
        color = MaterialTheme.colors.onSurface.copy(alpha = ContentAlpha.medium),
    )
```

> 说明：`import app.settings.UiScalePrefs` 已在 Step 1 加入；`Slider`、`Text` 等 import 已存在。

- [ ] **Step 5: `Main.kt` 打开快照 + 保存/取消接线**

import 区加：

```kotlin
import app.settings.UiScalePrefs
import app.state.UiScaleState
```

（Task 3 可能已加 `UiScaleState`，去重。）

在 `var editorSettings by remember { mutableStateOf(EditorPrefs.load()) }` 附近加：

```kotlin
    // 打开设置时的缩放快照：用于「取消」回退；实时预览改的是 UiScaleState
    var settingsInitialScale by remember { mutableStateOf(UiScaleState.scale) }
```

找到 `onOpenSettings`：

```kotlin
                        onOpenSettings = { dialogState.showSettings = true },
```

改为：

```kotlin
                        onOpenSettings = {
                            settingsInitialScale = UiScaleState.scale
                            dialogState.showSettings = true
                        },
```

找到 `SettingsDialog(...)` 调用，改为：

```kotlin
                SettingsDialog(
                    visible = dialogState.showSettings,
                    theme = activeTheme,
                    language = effectiveLang,
                    initial = SettingsSnapshot(editorSettings, keymap, languagePref, settingsInitialScale),
                    onDismiss = {
                        // 取消/关闭：回退实时预览的缩放，不落盘
                        UiScaleState.set(settingsInitialScale)
                        dialogState.showSettings = false
                    },
                    onSave = { snapshot ->
                        editorSettings = snapshot.editor
                        keymap = snapshot.keymap
                        languagePref = snapshot.language
                        // 非 UI 层读的是全局 I18n.lang；与 snapshot 同步更新，避免一帧旧语言
                        I18n.lang = snapshot.language ?: Lang.system()
                        // 全局界面缩放：即时生效 + 落盘
                        UiScaleState.set(snapshot.uiScale)
                        UiScalePrefs.save(snapshot.uiScale)
                        EditorPrefs.save(snapshot.editor)
                        KeymapPrefs.save(snapshot.keymap)
                        LanguagePrefs.save(snapshot.language)
                        dialogState.showSettings = false
                    },
                    onManageThemes = { dialogState.showThemeDialog = true },
                    onUiScalePreview = { UiScaleState.set(it) },
                )
```

- [ ] **Step 6: 编译 + 测试**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle compileKotlin && gradle test
```

Expected: 编译无警告错误；测试全绿。

- [ ] **Step 7: 人工验证（交给用户）**

1. 设置 → 通用设置 → 拖动「界面缩放」到 150%：主窗口与设置窗**同时**放大，设置窗内容不溢出/不被裁。
2. 点「取消」→ 界面回到打开设置前的比例；再打开 → 滑块回到原值。
3. 调到 150% 保存 → 重启 → 仍为 150%；`~/.local/share/db-k/app.properties` 含 `ui.scale=1.5`
   且 `language` key 未丢失。
4. 中英各切一遍：`界面缩放（150%）` / `UI scale (150%)` 无残留、无占位符错位。

- [ ] **Step 8: 提交**

```bash
git add src/main/kotlin/app/dialog/SettingsDialog.kt src/main/kotlin/app/core/Main.kt
git commit -m "feat(ui-scale): 设置窗口缩放滑块（实时预览/取消回退/保存落盘）"
```

---

### Task 5: 其余独立对话框接入缩放

**Files:**
- Modify: `src/main/kotlin/app/dialog/EditCellDialog.kt`
- Modify: `src/main/kotlin/app/dialog/ThemeDialog.kt`
- Modify: `src/main/kotlin/app/dialog/ViewerDialogs.kt`

**Interfaces:**
- Consumes: Task 3 `ScaledDialogWindow`、`scaledSize`。
- Produces: 全部 `DialogWindow` 均在缩放密度下渲染且窗口尺寸同比放大。

全部 4 个文件里 `DialogWindow(` 只出现在这 7 处（`EditCellDialog:74`、`ThemeDialog:131`、
`ViewerDialogs:305/375/500/663`），统一改法：`DialogWindow(` → `ScaledDialogWindow(`，
尺寸包 `scaledSize(...)`。

- [ ] **Step 1: `EditCellDialog.kt`**

import 区：删 `import androidx.compose.ui.window.DialogWindow`，加：

```kotlin
import app.ui.ScaledDialogWindow
import app.ui.scaledSize
```

（`rememberDialogState` import 保留。）

找到：

```kotlin
    DialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = windowSize),
```

改为：

```kotlin
    ScaledDialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = scaledSize(windowSize)),
```

- [ ] **Step 2: `ThemeDialog.kt`**

import 区：删 `import androidx.compose.ui.window.DialogWindow`，加：

```kotlin
import app.ui.ScaledDialogWindow
import app.ui.scaledSize
```

找到：

```kotlin
    DialogWindow(
        onCloseRequest = onDismiss,
        title = t(Str.ThemeManagerTitle),
        state = rememberDialogState(width = 780.dp, height = 560.dp),
    ) {
```

改为：

```kotlin
    ScaledDialogWindow(
        onCloseRequest = onDismiss,
        title = t(Str.ThemeManagerTitle),
        state = rememberDialogState(size = scaledSize(780.dp, 560.dp)),
    ) {
```

- [ ] **Step 3: `ViewerDialogs.kt`**

import 区：删 `import androidx.compose.ui.window.DialogWindow`，加：

```kotlin
import app.ui.ScaledDialogWindow
import app.ui.scaledSize
```

三处（`DdlDialog`、`TextViewerDialog`、`JsonViewerDialog`，均为 `VIEWER_WINDOW_SIZE`）：

```kotlin
    DialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = VIEWER_WINDOW_SIZE),
        title = title,
        resizable = true,
        onPreviewKeyEvent = escapeCloses(onDismiss),
    ) {
```

改为（三处相同替换）：

```kotlin
    ScaledDialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = scaledSize(VIEWER_WINDOW_SIZE)),
        title = title,
        resizable = true,
        onPreviewKeyEvent = escapeCloses(onDismiss),
    ) {
```

一处（`CommitPreviewDialog`）：

```kotlin
    DialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = DpSize(720.dp, 520.dp)),
        title = I18n.t(Str.CommitPreviewTitle, request.statements.size),
        resizable = true,
        onPreviewKeyEvent = escapeCloses(onDismiss),
    ) {
```

改为：

```kotlin
    ScaledDialogWindow(
        onCloseRequest = onDismiss,
        state = rememberDialogState(size = scaledSize(DpSize(720.dp, 520.dp))),
        title = I18n.t(Str.CommitPreviewTitle, request.statements.size),
        resizable = true,
        onPreviewKeyEvent = escapeCloses(onDismiss),
    ) {
```

- [ ] **Step 4: 确认无遗漏**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
# 排除包装器自身，只看真正的 DialogWindow 调用
grep -rn "DialogWindow(" src/main/kotlin --include=*.kt | grep -v "ScaledDialogWindow"
grep -rn "ScaledDialogWindow(" src/main/kotlin --include=*.kt
```

Expected: 第一条命令只剩 `app/ui/UiScale.kt` 内部对 `DialogWindow(...)` 的 1 处调用；
第二条为 7 处调用 + 1 处定义（`app/ui/UiScale.kt`）。

- [ ] **Step 5: 编译 + 测试**

```bash
export JAVA_HOME=/home/zk/.sdkman/candidates/java/25.0.3-jbr
export PATH="$JAVA_HOME/bin:/home/zk/.sdkman/candidates/gradle/9.4.1/bin:$PATH"
gradle compileKotlin && gradle test && gradle smokeJdbc
```

Expected: 编译无警告错误；`gradle test` 全绿；`gradle smokeJdbc` PASS。

- [ ] **Step 6: 人工验证（交给用户，150% 下）**

1. 单元格编辑弹窗（结果区双击单元格）放大且内容不裁；`Ctrl+Enter`/`Esc` 正常。
2. 查看器弹窗（表 DDL / 历史 SQL / JSON / 提交预览）放大且滚动正常。
3. 主题管理弹窗放大且内容不裁。
4. 命中对齐：树节点展开/右键菜单、下拉菜单（主题/工作区/新建）、结果筛选/表头菜单、
   Tooltip、补全弹层——文字与命中区域同步放大、位置不偏移。
5. 中英 + 深色/浅色各看一遍：无残留文案、无黑字沉底/过曝白块。

若发现某类弹层未放大：记录入口（如「树右键菜单」），下个增量对其单独 `ProvideUiScale` 包裹。

- [ ] **Step 7: 提交**

```bash
git add src/main/kotlin/app/dialog/EditCellDialog.kt src/main/kotlin/app/dialog/ThemeDialog.kt src/main/kotlin/app/dialog/ViewerDialogs.kt
git commit -m "feat(ui-scale): 单元格编辑/主题/查看器弹窗接入缩放"
```

---

## Self-Review

**1. Spec coverage**
- 方案 A / 只乘 density：Task 3。
- 默认 100% 零回归：Task 1 `DEFAULT_SCALE`、Task 3 `UiScaleState` 初值。
- 全部窗口覆盖：Task 3（主窗口）、Task 4（设置）、Task 5（其余 3 文件 6 处 + 设置窗已在 Task 4）。
- 幂等（DialogWindow 继承）：Task 3 `LocalUiScale` + `scaledDensity` + `UiScaleTest`。
- 持久化 `app.properties ui.scale`：Task 1。
- 设置界面实时预览/取消回退/保存落盘：Task 4。
- 默认窗口尺寸随 scale：Task 3 Step 6(c)。
- i18n zh+en：Task 2。
- 明确不做项：无对应任务（符合预期）。

**2. Placeholder scan**：无 TBD/TODO；所有代码步骤均为可直接粘贴的完整代码。

**3. Type consistency**：`UiScalePrefs.{MIN_SCALE,MAX_SCALE,STEP,DEFAULT_SCALE,sanitized,load,save}`、
`UiScaleState.{scale,set}`、`scaledDensity`、`ProvideUiScale`、`scaledSize(Dp,Dp)`、`scaledSize(DpSize)`、
`ScaledDialogWindow`、`SettingsSnapshot.uiScale`、`SettingsDialog.onUiScalePreview` 在定义任务与使用任务间命名一致。

**4. Review Focus 落点**：见文首 Review Focus，5 项均已在对应 Task 的测试/人工验证步骤中覆盖
（1→Task 3/5，2→Task 5，3→Task 4/5，4→Task 4，5→Task 1）。
